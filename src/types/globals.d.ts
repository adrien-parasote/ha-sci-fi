/**
 * Compile-time constants injected by @rollup/plugin-replace.
 * Values are substituted at build time — never available at runtime as variable references.
 */
declare const __VERSION__: string;
declare const __DEV__: boolean;
